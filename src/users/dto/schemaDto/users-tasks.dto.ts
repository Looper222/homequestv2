export class UsersTasks {
    _id: string;
    childnames: string;
    title: string;
    description: string;
    flashes: number;
    _type: number;
    //priority: number (this will be enum)
    //time: date
    //avatar: number (number of avatar stored in app)
}